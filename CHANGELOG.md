- [4.1.10] - 2026-09-21 — every block now tells the agent who stopped it and how to proceed. permissionDecisionReason is the only part of a block a model receives - the banner and remediation line hooks write to stderr reach the human and stop there, so an agent that hit a guard could not tell policy from a shell error and retried blindly. hooks/lib-deny.sh is now the single emitter for 39 of 40 decision sites, carrying attribution, the reason, and a way through. Six fail-open shapes closed on the way: guards that emitted NO decision at all when the fork escaping their reason failed, including one in a branch written to fail closed and one that silently skipped the default-branch prompt entirely. 5801 tests passing.
- [4.1.9] - 2026-09-20 — security: install scanners missed the installs agents actually write — 'cd app && npm i <pkg>' reached neither the credibility check nor the vulnerability audit, through a start-anchored regex and a pre-gate that made the short verb form unreachable. Also: the documented 'eco <tier>' switch now actually switches, and the economy tier has one owner. 5789 tests passing.
- [4.1.8] - 2026-09-19 — Three economy hooks read a context-window field that no hook event payload carries, so none had fired in 62 sessions; the statusline now publishes the percentage to a per-session sidecar the hooks read. economy-reinforce now fires every prompt instead of twice a session. 5771 tests passing.
- [4.1.7] - 2026-09-19 — Security: the metadata-text exemption was anchored to the start of the command, so a credential read chained after a commit (git commit -m x && cat <cred>) and a substitution inside a commit message both went unscanned; the same line also denied ordinary compound commits whose message text merely mentioned a credential filename. Replaced in all three sites with a segment-level strip. Denial-message audit of all 22 deny-emitting hooks: budget-cap now names the configurable cap, env-file-guard puts its remedy in the reason channel rather than stderr only.. 5761 tests passing.
- [4.1.6] - 2026-09-18 — Out-of-project denials now name additionalRoots, the scoped way through, instead of only the blanket opt-out; a single parent root covers every directory beneath it. Deny-rule false-positive audit (P1): a search pattern is no longer read as a filename. Guard-independence pins (P2). Live CI and test-count badges.. 5753 tests passing.
- [4.1.5] - 2026-09-15 — detect Unicode Tag Block (U+E0000-E007F) ASCII smuggling. 5721 tests passing.
- [4.1.4] - 2026-09-15 — Block disabling the host's own security controls (Gatekeeper, SIP, SELinux, firewall, Windows Defender, AppArmor, auditd), scoped to the disable verb. From the SkillGuardrail audit.. 5720 tests passing.
- [4.1.3] - 2026-09-15 — CI now triggers on rel/** pushes so the stage/promote release flow self-gates without a manual PR.. 5707 tests passing.
- [4.1.2] - 2026-09-14 — Block gpg secret-key export exfil (agentjail audit) and the raw-device-destruction / partition-editor sibling family — blkdiscard, mke2fs, nvme format, cfdisk, sgdisk, gpart, hdparm secure-erase, shred-on-device, sysrq-trigger (heinzel audit).. 5707 tests passing.
- [4.1.1] - 2026-09-14 — Two guard-bypass fixes from a multi-lens review: harness-tamper-guard copy-family (cp/install/rsync/scp) no longer fails open on a trailing redirect/comment; smart-approve no longer auto-approves curl upload forms (-F/--form, -T/--upload-file, --data-urlencode, glued -d@file) as GET-only. 5670 tests passing.
