## Handoff — claude-supercharger — 2026-09-17

### Done
- **Community research sweep** → `docs/COMMUNITY-WANTS-2026-09-17.md` (merged, #19).
  Reaction-ranked `anthropics/claude-code` issues plus web, filtered through the
  enforcement-hook thesis, with plans P1–P5.
- **P1 shipped** (#18): two real false-positive defects in our own guards. Reasoning
  is in the commit message and the PR; not repeated here.
- **P2 shipped** (#20): `tests/test-guard-independence.sh` pins that guards are
  independent of the harness permission layer, with a negative control.
- **P3 answered by measurement**: PreToolUse hooks DO fire for a subagent's own
  Bash calls. No code change; no limitation to document.
- **P4 reproduced and closed**: upstream #90450 is real, and we are immune by
  construction. No code change.
- **P5**: no build by design.
- **All three PRs merged**; master is `54f7c8b`, clean, zero open PRs.
- awesome-claude-code #2096 unchanged this session: OPEN, `validation-passed`,
  no maintainer reply since the 2026-09-16 answer.

### In Flight
- **Master CI for `54f7c8b` was still `in_progress`** when this brief was written
  (run 35208104003). Nothing depends on it except the published test-count badge.

### Decisions Made
- **Merge order #19 → #18 → #20**: docs first, guard change second, test-only
  corpus last, so any rebase lands on the cheapest branch. Nothing actually
  needed rebasing.
- **Squash merges with branch deletion**, matching the existing master history.
- **P2 was scoped DOWN after reading the repo.** The plan's original framing was
  already covered three times over (autopilot safety floor in `test-autopilot.sh`,
  the user's own deny/ask in `test-smart-approve-user-deny.sh`, the loosening
  confirms in `test-loosening-modes-confirm.sh`). Only the product claim itself
  was unpinned, so that is all the new file asserts.
- **P3 tested the subagent arm only.** A skill's instructions load into the parent
  loop, so its tool calls are the parent's calls and hook firing there is already
  demonstrated continuously. The subagent was the only genuine unknown.

### What Failed
- **Our own guards denied four probe commands**, twice destroying a heredoc
  mid-write so the NEXT call failed with "No such file or directory" — which reads
  like a missing file, not a block. Also hit: editing a copy named
  `hooks/safety.sh` (self-modification rule) and a nested `bash -c` (dangerous
  pattern rule). All were retried in another form; none should be loosened.
- **I called Windows CI "stalled in a queue" after ~1h.** Wrong: the job had
  started 08:43:42Z and was running normally. I was measuring from PR creation,
  not job start. Corrected in the same session.
- **The first CI monitor wasted its 30-minute window** emitting jobs that were
  already green, because its filter was "any terminal check" rather than "the
  Windows check". Re-armed with the narrow filter and it worked.
- `gh issue list --search "deleted files OR overwrote OR data loss"` silently
  ignored the keywords and returned the unfiltered reaction-ranked list.

### Blockers
- none.

### Files Touched
All merged to master and recoverable from the diffs: `hooks/safety-detect.py`,
`hooks/env-file-detect.py`, `tests/test-deny-rule-fp.sh`,
`tests/test-guard-independence.sh`, `docs/COMMUNITY-WANTS-2026-09-17.md`.

### Resume With
The P1–P5 plan in `docs/COMMUNITY-WANTS-2026-09-17.md` is fully discharged and
merged. The two open follow-ups are the agent-visible deny reason (no attribution
or remediation reaches the agent) and a v4.1.6 comment sweep if the next release
is a minor. A release is the natural next step.

### Start With
`/sc-status` — confirm the local install matches the new master before anything
else; the global install was on v4.1.5 and master now carries three unreleased
merges.

---

### Measurements
- **P1 corpus pre-fix: 16 passed, 4 failed.** The four: a pattern-form search, and
  three template reads (`.example`, `.template`, `.sample`), all failing on
  `safety.sh` only. Post-fix 20/20.
- **Full suite on the P1 branch: 5741 passed, 0 failed**, `bash tests/run.sh`
  exit 0. On the P2 branch (off master, without P1): **5727 passed, 0 failed**,
  exit 0. Baseline was 5721.
- **Merged total is 5747 by arithmetic (5721 + 20 + 6) and was NOT measured.**
  The master CI run for `54f7c8b` publishes the real number to the badge gist —
  read it there rather than trusting this line.
- **Channel asymmetry, pre-fix:** `env-file-guard.sh` allowed all three template
  reads; `safety.sh` denied all three. The pattern-form FP denied on BOTH.
- **`rg` and `egrep` pattern forms were already allowed pre-fix**, because
  `_ENV_READ_WRITE_PREFIXES` contains `\bgrep\s+` and `\b` does not match inside
  `egrep`, while `rg` is absent from that list. F1 manifested through literal
  `grep` only; the fix generalises it to all nine pattern-readers.
- **Windows CI job duration: 1h 01m 55s** (PR #18, run 35201235375). ubuntu test
  suite 5m24s, macOS 8m37s–12m00s. This is why Windows looks stuck and is not.
- **P3 probe**: one `general-purpose` subagent, two Bash calls, neither file
  existing. Control returned `No such file or directory`; probe returned the
  guard's reason with no shell error, proving the call never reached `cat`.
- **P4 probe**: same file, two access methods. Bash `cat` → nested `CLAUDE.md`
  did not load. `Read` tool → it loaded immediately, sentinel visible.

### Rejected — do not rebuild
- **Importing `_strip_pattern_operands` into `env-file-detect.py`** rather than
  duplicating: the hyphenated module name is not importable without `importlib`,
  and that file exists to keep the Bash path fork-free.
- **Fixing the pattern FP in `env-file-guard.sh`** (the shell layer): would have
  fixed one channel and left `safety.sh` denying.
- **A general "ignore quoted text" rule** for the pattern FP: already rejected
  upstream in v2.26.68's own comment, and it would allow a search whose sensitive
  name IS a genuine file argument.
- **Re-testing the adjacent P2 invariants** (autopilot floor, user deny/ask,
  loosening confirms): all three already have dedicated suites.
- **Prefixing every deny reason with attribution** was considered and deferred,
  not rejected — see Open questions.

### Dead ends
- **`hooks/path-guard.sh` is not part of the deny-rule FP class.** It matches
  `Write|Edit|MultiEdit|NotebookEdit` only and never sees a Bash command. Verified
  false; do not re-audit it for this.
- **The five upstream `cd`-compound shapes all already passed** against both our
  guards. They are pinned now, but there was no defect there.
- **`gh issue list` keyword search** cannot filter by free text this way; use
  label slices or a `created:>=` recency slice.
- **Editing a copy of a guard to build a negative control**: blocked by the
  self-modification rule because of the filename. The working form is a stub hook
  under a NON-guard name (`stub-that-never-denies.sh`) plus a `sed`-rewritten copy
  of the test — that proved the deny assertion fails correctly.

### Open questions
- **Deferred, not unknown**: the agent-visible deny reason carries no attribution
  and no remediation. Verified in `hooks/env-file-guard.sh`'s `block()` — the JSON
  sends the reason alone while the `Supercharger blocked …` banner and the "run it
  in your terminal" line go to stderr, which the agent never sees. A subagent can
  read a block as a plain failure. Fixing it touches every deny message and the
  tests asserting on them, so it wants its own branch.
- **Unverified**: whether `CLAUDE_CODE_THRIFTY_SONIC=false` actually disables the
  Auto Mode Bash-first steer. Upstream reports it; not tested here.
- **Open**: whether the next release is 4.1.6 or 4.2.0. The new code comments are
  stamped v4.1.6 and need a sweep if it is a minor.
- **Unverified**: the merged suite total (see Measurements).

### Reproduce
- `bash tests/test-deny-rule-fp.sh` — 20 cases, both channels per case.
- `bash tests/test-guard-independence.sh` — 6 cases including the negative control.
- Prove the P2 corpus can fail: copy it, `sed` one hook name to a stub that exits
  0, run it against a temp dir holding that stub plus `helpers.sh`. It reports the
  failure with the right message.
- `bash tests/run.sh` — do NOT pipe it (a pipe reports the pipe's status), and
  take the LAST `Total:` line (an inner test echoes a decoy).
- P4: make a fresh subdirectory with a sentinelled `CLAUDE.md` and an ordinary
  file, `cat` the file via Bash (no load), then open the SAME file with the Read
  tool (loads). Directory must be untouched this session — loading is once per
  directory.
- Upstream evidence: `gh issue list --repo anthropics/claude-code --state open
  --label area:permissions --search "sort:reactions-+1-desc"`.
