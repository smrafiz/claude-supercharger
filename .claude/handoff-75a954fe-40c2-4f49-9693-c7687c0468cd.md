## Handoff — claude-supercharger — 2026-09-21

### Done
- **v4.1.9 released** (`cf6d6d8`) and **v4.1.10 released** (`41e59d0`), both via
  stage/promote, both 8/8 CI including Windows.
- **v4.1.9**: install-scanner bypass (security), the `eco <tier>` switch made real
  with one owner for the tier, carry file, economy-layer doc correction.
- **v4.1.10**: every decision now emits through `hooks/lib-deny.sh` with
  attribution and a remedy. 39 of 40 sites; `lib-stdin.sh` the one exemption.
- **Six fail-open shapes closed** by that migration — see the commit messages on
  `e2da789` and `33220d8`; the class is recorded in memory.
- **config-scan FP fixed** → PR **#31**, open, CI running.
- Carry file refreshed twice (#26, #27); economy doc updated with the
  output-styles finding (#25).

### In Flight
- **#31** (config-scan FP) — 6/8 green, a test suite and Windows pending.
- **#22** — held by choice, not CI. Waits on the Concise measurement.
- **This machine is on v4.1.9, not v4.1.10.** `lib-deny.sh` is absent from
  `~/.claude/supercharger/hooks/`. Needs `/sc-update`.

### Decisions Made
- **Scope of the emitter migration was the user's call, not mine.** Offered
  helper-only / firing-hooks-only / all 22; they chose all 22, then the 21 ask
  sites followed. Worth knowing the full migration was authorised deliberately.
- **Escaping in pure bash**, not a fork. Not for speed — a deny is rare — but
  because a guard that cannot emit its verdict for want of an interpreter fails
  OPEN. That reasoning is what removed six fallback branches.
- **Promote v4.1.10 before landing the FP fix**: the release had been staged and
  gated for an hour; folding one more commit in meant another full Windows cycle.
- **config-scan fixed positionally, not with another name list.** An injection
  gives the verb as a command; a description puts a subject in front of it.

### What Failed
- **PR #29 was closed, not retargeted.** Squash-merging its base branch (#28) and
  deleting that branch closed the stacked PR. Recovered by cherry-picking the one
  commit onto fresh master and opening #30. **Next stack: retarget the child to
  master BEFORE merging the parent, or do not delete the base on merge.**
- **A suite run I nearly reported was invalid.** I created a test file while the
  suite was running; the runner picked it up mid-flight, the log ended at its
  header, and it still exited 0. Re-ran on a stable tree.
- **I wrote a vacuous assertion** — `case "$R" in "Supercharger: "*|"") pass` —
  whose `""` arm meant it passed when the guard never fired. Caught by applying
  the instrument-blindness question to my own new test.
- **I attributed a guard denial to `tail -1` without testing it**, and put that in
  a PR body. Five reconstructions, including a faithful replay, all pass. Cause
  still unknown; the claim was retracted and the branch I opened to "fix" it
  deleted.

### Blockers
- none.

### Files Touched
All merged or in #31; recoverable from the diffs. New this session:
`hooks/lib-deny.sh`, `tests/test-deny-attribution.sh`,
`tests/test-config-scan-prose.sh`.

### Resume With
v4.1.10 is out and this machine has not picked it up. Run `/sc-update`, then land
#31 when Windows clears. The only real open question is whether the Concise output
style binds where `economy.md` did not, which decides #22.

### Start With
`/sc-update` — the machine is a release behind, so `lib-deny.sh` and every
attributed deny message are not actually live here.

---

### Measurements
- **Suite growth this session**: 5781 → 5800 (deny attribution, +19) → 5801 (ask
  migration, +1 net) → **5813** (config-scan prose, +12). Every figure from
  `bash tests/run.sh` with the exit code checked separately, never piped.
- **Red-first counts**, each against master in a detached worktree:
  deny attribution 5 of 12 fail; ask migration 5 of 12; config-scan prose 5 of 12.
  In each case the passing ones are the false-positive guards, which must hold on
  both sides.
- **Decision-site census before migration**: 58 sites across 53 files — 24 deny,
  34 ask. Escaping was 85 × `python3 json.dumps` (~20ms) and 49 × `jq -Rs` (~6ms).
  After: 39 of 40 through the helper, zero forks on the decision path.
- **Windows CI duration**: rel/4.1.10 started 10:22:29Z, run completed by 11:47Z.
  Consistent with the 1h01m55s measured on 2026-09-17. It looks hung and is not —
  check `startedAt`, not PR creation time.
- **Context census** (`/context`, Opus 5 1M): resident floor 107.5k tokens ≈ 10.8%
  — system tools 40.2k, MCP 35.4k, memory files 13.4k, skills 9.9k, system prompt
  4.5k, agents 4.1k. Supercharger's own share ≈ 14.6k (1.5%), of which `MEMORY.md`
  alone is 9.4k. `webstorm` MCP was ~20k and has been disconnected here.
- **Economy baseline for the Concise comparison**: median **207** chars, mean 447,
  over 1,597 assistant messages before the switch.

### Rejected — do not rebuild
- **Migrating only the hooks the telemetry shows firing** (~6 of 22). Offered and
  declined in favour of the full set; partial migration is the drift shape this
  repo keeps paying for.
- **Fixing the config-scan FP with a name list or an allowlist of phrases.** The
  distinction is grammatical position, not vocabulary; a list would need an entry
  per way of describing the feature.
- **A narrowing for the unexplained `sensitive file access` denial.** No
  reproduction after five attempts, so there is no defect to fix. Recording the
  guess would have been worse than recording nothing.
- **Re-staging v4.1.10 to include the FP fix.** Another hour of Windows for a
  cosmetic gain.

### Dead ends
- **Reproducing the guard denial that blocked this session's own commit.** Tried:
  heredoc body plus `tail -1`; `gh pr create --body` with the literal; `cd && git
  commit -F -` heredoc; a faithful full-command replay. All pass. Only
  `safety-detect.py` emits that string and it fires on none of them.
- **Reading Windows per-assertion results from the CI step log.** They are not
  there; the detail lives in the `windows-suite-log` artifact, fetched with
  `gh api .../artifacts/<id>/zip`. That is the route to use next time.
- **`ls -t` to find the newest handoff brief.** git rewrites mtimes on checkout,
  so it picked a 2026-09-07 file as "newest". Sort by the date on line 1.

### Open questions
- **Does the Concise output style bind where `economy.md` did not?** Unverified
  and now measurable. Needs several ordinary build sessions, then a comparison
  against the 207-char baseline. Decides #22.
- **What actually caused the one-off `sensitive file access` denial?** Unknown,
  not verified-false. If it recurs, capture the exact command before anything else.
- **Machine B's state** is unknown from here; it was a release behind as of
  2026-09-20 and nothing has confirmed an update since.
- **Verified false**: that a release promote reinstalls the local copy. It did not
  for v4.1.9 or v4.1.10 — checked both times by looking for the new files.

### Reproduce
```bash
# the suite, never piped — a pipe reports the pipe's status
bash tests/run.sh > /tmp/suite.log 2>&1; echo "rc=$?"
grep -aE "^Total:" /tmp/suite.log | tail -1     # LAST line; an inner test echoes a decoy

# red-first, without touching the working tree
WT=$(mktemp -d)/old; git worktree add -q --detach "$WT" master
cp tests/<newtest>.sh "$WT/tests/"; bash "$WT/tests/<newtest>.sh" "$WT"
git worktree remove --force "$WT"

# Windows per-assertion detail (NOT in the step log)
id=$(gh run list --branch <branch> --limit 1 --json databaseId --jq '.[0].databaseId')
aid=$(gh api repos/smrafiz/claude-supercharger/actions/runs/$id/artifacts --jq '.artifacts[0].id')
gh api repos/smrafiz/claude-supercharger/actions/artifacts/$aid/zip > a.zip && unzip -qo a.zip
sed 's/\x1b\[[0-9;]*m//g' win-suite.log | grep -A14 -- "--- <testname> ---"

# is the installed copy current?
grep -m1 -oE 'Claude Supercharger v[0-9.]+' ~/.claude/CLAUDE.md
ls ~/.claude/supercharger/hooks/lib-deny.sh 2>/dev/null || echo "not installed"

# economy comparison (baseline 207 median / 447 mean over 1,597 messages)
# median assistant-message length per transcript, newest 6 sessions
```
