## Handoff — claude-supercharger — 2026-09-16

### Done
- **Shipped v4.1.5** (`15450ff`, tag + GitHub release, all 8 CI jobs green incl.
  Windows). Unicode Tag Block (U+E0000–E007F) ASCII-smuggling detection — the aigis
  finding, PR #15. Cut via stage/promote.
- **Global install updated 4.0.48 → 4.1.5** (`/sc-update`); backup at
  `~/.claude/backups/20260916-094931/`. CLAUDE.md now stamps v4.1.5.
- **Live badges (PR #16 `d334f9e`, PR #17 fix `0352ffa`)** — replaced the hard-coded
  `tests-5721 passing` README badge with (a) a GitHub Actions CI-status badge and (b) a
  live test-count badge published to a gist by CI. Both on master. Badge now reads
  `tests 5721 passing` (verified via the gist raw URL).
- **awesome-claude-code #2096 updated + validation PASSED** — shortened description,
  removed all stale magic numbers (the maintainer's request), answered his Auto Mode
  question, changed Category `Hooks`→`Security` with sub-category BLANK (their taxonomy
  changed; `Hooks` no longer exists). Reply comment posted. Now awaiting maintainer review.

### In Flight
- none. Working tree clean, master = `0352ffa`, no open PRs.

### Decisions Made
- **Badge published on master pushes only, from ubuntu only** (`ci.yml` `if:` guard) —
  so the gist isn't written twice per run and PR/rel branches don't churn it.
- **Category = Security (no sub-category)** for the awesome-claude-code listing — `Hooks`
  was removed from their taxonomy; Security is the sharpest fit and the one the
  validation task demonstrates. Runner-up was Multi-Purpose.
- **Gist endpoint badge over in-repo JSON** — no bot-commit churn, no secret in the
  repo; needs only a repo secret + variable (both set by the user).

### What Failed
- **Badge count published `6 passing`, not `5721`** (PR #16 first master run,
  `35060933683`). `ci.yml` extract used `head -1` on `grep 'Total: [0-9]+ passed'`; a
  test echoes a decoy `Total: 6 passed` in its own output, so head grabbed that instead
  of run.sh's authoritative LAST `Total:` line. Fixed PR #17 with `tail -1` (verified
  locally against a decoy+real sample; then confirmed on master run `35069405784` →
  gist `5721 passing`). This is the exact trap `tests/run.sh:150` already documents.

### Blockers
- none. Ball is in maintainer @hesreallyhim's court on #2096.

### Files Touched
Recoverable from PRs #15/#16/#17 diffs. Not recoverable:
- `.github/workflows/ci.yml` — added "Extract suite total" + "Publish tests badge to
  gist" steps to the `test` job, gated `matrix.os==ubuntu && event==push && ref==master`.
  Uses `schneegans/dynamic-badges-action@v1.7.0` with `secrets.GIST_TOKEN` +
  `vars.TESTS_BADGE_GIST_ID`. Run tests step now `| tee test-output.log`.
- `README.md:5` — CI-status badge + shields `endpoint` count badge (gist
  `b57e04622039cbdf04f2a2ed338d36e7`, file `supercharger-tests.json`).

### Resume With
claude-supercharger is idle at master `0352ffa` (v4.1.5), clean, no open PRs. Live CI +
test-count badges are working (gist shows `5721 passing`). awesome-claude-code #2096
passed validation and awaits maintainer review — do nothing there unless he replies. The
repo-audit sweep can resume on a pasted GitHub URL.

### Start With
none.

---

### Measurements
- Badge extract bug: `head -1` → `6`, `tail -1` → `5721`, proven on a synthetic log with
  a decoy `Total: 6 passed` before the real `Total: 5721 passed`
  (scratchpad/sample.log). Real total confirmed live: gist message `5721 passing`.
- shields endpoint renders the gist: `curl .../img.shields.io/endpoint?url=<gist-raw>` →
  `<title>tests: passing</title>` (placeholder) then `5721 passing` after CI publish.
- v4.1.5 Windows CI: cleared green at ~1h12m (run `34952909084`) — same whole-day GitHub
  infra lag as v4.1.3/4.1.4; promote gate held without bypass.

### Rejected — do not rebuild
- **In-repo badge JSON committed by CI** — a bot commit per master push (churn) and it's
  the same magic-number-in-a-file the maintainer warned against, just auto-bumped. Gist
  endpoint avoids both.
- **Keeping the static count badge** — that IS the stale-number the maintainer flagged.
- **Category Multi-Purpose or Runtime Infra** for #2096 — Security is sharper and matches
  the validation task (destructive-command denial).

### Dead ends
- Reading the ubuntu job log for the `Total:`/extract lines via `gh run view --job --log`
  returned empty for the count lines (ANSI/format); faster to reproduce the extract
  locally against a synthetic log and read the published gist directly.

### Open questions
- **Does the count badge stay correct across releases?** Verified once (5721). The
  `tail -1` fix is sound but only one master run has exercised it. If a FUTURE test file
  prints a `Total: N passed` line AFTER run.sh's grand total, tail would catch the wrong
  one — currently nothing prints after `run.sh:166`.

### Reproduce
```bash
# badge value the gist serves (what the README shows):
curl -sS "https://gist.githubusercontent.com/smrafiz/b57e04622039cbdf04f2a2ed338d36e7/raw/supercharger-tests.json"
# extract-logic check (head=decoy, tail=real):
printf 'x\nTotal: 6 passed, 0 failed\n===\nTotal: 5721 passed, 0 failed\n' > /tmp/s.log
grep -oE 'Total: [0-9]+ passed' /tmp/s.log | grep -oE '[0-9]+' | tail -1   # -> 5721
# CI publish step ran?
gh run view <master-run-id> --json jobs -q '.jobs[]|select(.name|test("Test suite .ubuntu"))|.steps[]|select(.name|test("badge|total";"i"))|"\(.name): \(.conclusion)"'
```
