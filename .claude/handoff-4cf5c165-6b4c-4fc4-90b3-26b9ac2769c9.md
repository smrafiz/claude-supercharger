## Handoff — claude-supercharger — 2026-09-07

Baseline `ed72448` (v4.0.30). Three commits on top; `4df6cba` and `21b6434` pushed,
**`b4bf65b` is not**. Tree clean, suite green. Commit messages carry the reasoning —
this brief covers only what they cannot.

### Done
- **Perf figures re-measured and published** (`4df6cba`). README's speed table and
  `docs/perf-baseline.json` both predated `bab5d53`, the fix to a harness that was
  measuring 2 of 18 hooks. HOOK-LATENCY-PLAN Phase 4 now closed.
- **KNOWN-ISSUES #6 narrowed** (`21b6434`) — fetch-gated structural tier in
  `hooks/bash-injection-scanner.sh`. 4 of 6 recorded shapes now warn.
- **KNOWN-ISSUES #5 narrowed** (`b4bf65b`) — `_expand_sensitive_bindings` in
  `hooks/safety-detect.py`, applied before the dispatch chain.

### In Flight
- none. No half-finished edits, no running jobs, no background watches.

### Decisions Made
- **Both narrowings applied at the SHARED path, not the named call site.** #6's tier
  gate and #5's substitution each sit before the per-check dispatch. The entries
  each name one symptom; in both cases every sibling check had the same blind spot
  (`tar czf out.tgz $F` was as silent as `cat $F`).
- **#5 substitutes only bindings whose value is already sensitive.** That single
  constraint is the whole false-positive argument — an expansion can then only insert
  a token blocked literally anyway. Widening it to all bindings is the thing the
  original entry correctly refused.
- **Both entries stay OPEN, narrowed.** The unclosable halves are unattempted and
  the limits are asserted as tests, not left as prose.
- **Plan §7's 2026-07-31 measurement table was left as-is** with a dated re-measure
  beside it. A plan's numbers are a record of when they were taken.

### What Failed
- **First #6 gate used a second `grep`.** Correct, and +2.0 ms on EVERY Bash call.
  Replaced with fork-free bash `case`: +0.1 ms. See Measurements.
- **First perf comparison ran the old hook from `/tmp`.** `. ${BASH_SOURCE[0]%/*}/lib-timing.sh`
  failed there, `set -e` killed it, and it "measured" 2.67 ms — a hook that exited
  before doing anything. Copy the old version into `hooks/` as a sibling instead.

### Blockers
- none.

### Files Touched
- `hooks/bash-injection-scanner.sh` — tier-2 structural panel + fetch gate.
- `hooks/safety-detect.py` — `_expand_sensitive_bindings`, called once before dispatch.
- `tests/test-bash-injection-scanner.sh` — +40 assertions.
- `tests/test-var-bound-sensitive-path.sh` — new, 21 assertions.
- `docs/KNOWN-ISSUES.md` — #5 and #6 rewritten; original entries kept verbatim below
  each new header, because their arguments are still what stops the next attempt.
- `docs/perf-baseline.json`, `README.md`, `docs/HOOK-LATENCY-PLAN.md`, `docs/ROADMAP.md`.

### Resume With
claude-supercharger is clean at `b4bf65b` (unpushed) with 5399 assertions green.
Remaining backlog: triage PR #1 (`feat/windows-support`, draft since 2026-07-08,
26 unique commits, and master shipped its OWN Windows support since — the branch's
`python3.exe` shim is NOT on master), and prune 7 fully-merged remote branches.

### Start With
`/pr` if taking on PR #1 — the triage question is which of its 26 commits are still
unlanded, not whether to merge the branch as-is.

---

### Measurements
All on this M4 Pro, bash 3.2, 2026-09-07.

- **Hook chain, 19 hooks** (`bash tests/perf-chain.sh`): felt 6.8 ms, chain sum
  66.8 ms, spawn 38.0 ms (57%), work 28.8 ms, slowest `budget-cap.sh` 6.8 ms,
  spread 2.4–6.8 ms. Statusline cold 45.0 ms / warm 6.8 ms at 10 iterations.
- **CI ubuntu runner**, run `34027609454` job `101471275463`: felt 10.0 ms, chain
  sum 98.4 ms, spawn 38.0 ms, 19 hooks. Statusline cold 74.8 / warm 3.0 ms.
- **The +2.0 ms grep.** `bash-injection-scanner.sh` on an `ls -la` payload, 60 reps:
  old 5.02 ms/call, with a second grep 7.04 ms, with fork-free `case` 5.03 ms.
  A `docker compose config` payload gave 4.91 / — / 5.04. The 2 ms IS the spawn
  floor; it is not the grep's work.
- **#5's cost**: `safety-detect.py` on `CMD="ls -la"`, 30 reps: 31.81 ms before,
  32.24 ms after. Two module-level regexes and one finditer, no new process.
- **Structural panel FP re-measurement** (scratchpad `inj-probe2.py`): 4/6 injections
  caught, 3/12 ordinary stdout false-positive — same shape as the entry's recorded
  9/12 with its wider panel.

### Rejected — do not rebuild
- **Widening the tier-1 fast-path grep to carry tier-2 seeds.** It runs on every
  Bash call; the gate's whole value is exiting before python. Two gates chained,
  the second fork-free, keeps the common path at one grep.
- **Variable tracking that follows any binding** (#5). The original entry is right:
  mis-tracking becomes a block on ordinary work. Only sensitive-valued bindings.
- **Regexes for #6's last two shapes** — "exfil phrased normally", "install
  instructions". Textually identical to legitimate READMEs. Argument unchanged and
  preserved verbatim in the entry.
- **Optimising the hook chain at all** (HOOK-LATENCY-PLAN §7). Re-confirmed: two more
  hooks than the 2026-07-31 reading grew the spawn share 49% → 57%, so the half
  optimisation could reach got *smaller*.

### Dead ends
- **Looking for HOOK-LATENCY Phase 4's work as unstarted.** The README figure had
  already landed in `b8a9958`; the real task was that it was measured against a
  broken harness. Read `git log -S` on the claim before assuming a box is unticked.
- **`gh run view --job <id> --log | grep -i felt`** returns the `/perf` report, not
  the chain harness table. Use `sed -n '/felt (parallel)/,/Statusline render/p'`.

### Open questions
- **Is anyone using this besides the user?** Asked three sessions running, still
  unanswered. Verified-unknown, not verified-false. It outranks the backlog: it
  decides whether the next move is code or distribution.
- **PR #1's 26 commits** — unverified which are genuinely unlanded. Confirmed only
  that `python3.exe` shim is absent from master and that master's Windows CI is green.
- **Does the #6 fetch gate cover `gh api` output in practice?** Pattern is there and
  tested; never observed on a real poisoned payload.

### Reproduce
```
bash tests/perf-chain.sh --json                       # chain numbers
bash tests/perf-chain.sh --target statusline --iterations 10
bash tests/test-var-bound-sensitive-path.sh           # 21, #5
bash tests/test-bash-injection-scanner.sh             # 90, #6
gh run view --job 101471275463 --log | sed -n '/felt (parallel)/,/Statusline render/p'
```
Perf A/B: `git show HEAD:hooks/<hook> > hooks/.perf-old.sh` (sibling path REQUIRED —
`lib-timing.sh` is sourced relative to `$BASH_SOURCE`), loop 60 reps, `rm` after.
